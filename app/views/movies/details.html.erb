<%= render 'movie_show' %>
<p>IMDB Rating: <%= @movie.rating %></p>

<%= button_to "Save movie", save_movie_path(@movie.id, rating: 50) %>

<%= button_to "Fave movie", save_movie_path(@movie.id, rating: 100) %>